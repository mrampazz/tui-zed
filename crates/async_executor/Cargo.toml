[package]
name = "async_executor"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "async_executor.rs"

[dependencies]
futures.workspace = true
tokio = { version = "1", features = ["rt", "time", "macros"] }
